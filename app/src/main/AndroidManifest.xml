<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ultrapro4.techbasics">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechBasics">

        <!-- Splash Screen -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Main Dashboard -->
        <activity
            android:name=".MainActivity"
            android:exported="true"/>

        <!-- Other Activities -->
        <activity android:name=".InternetActivity" android:exported="false"/>
        <activity android:name=".WifiActivity" android:exported="false"/>
        <activity android:name=".BluetoothActivity" android:exported="false"/>
        <activity android:name=".CloudStorageActivity" android:exported="false"/>
        <activity android:name=".AIActivity" android:exported="false"/>
        <activity android:name=".QRActivity" android:exported="false"/>
        <activity android:name=".AppActivity" android:exported="false"/>
        <activity android:name=".VirusActivity" android:exported="false"/>

    </application>

</manifest>
